# Agent Skills

**让所有 AI Agent 一键拥有 Skills 能力，推动 Agent 自我进化**

## 愿景

我们相信 AI Agent 应该具备**自我进化**的能力——不仅能执行任务，还能学习新技能、创造新工具。

**Skills（技能）** 是实现这一愿景的核心机制：
- Agent 可以随时学习预先封装的专业知识和脚本
- Agent 可以根据需要创建全新的技能
- 技能以独立模块形式存在，可分享、可复用

**Agent Skills** 让任何 AI Agent 都能一键获得这种能力：

- 支持 **MCP 协议**：兼容 Claude Desktop、Cursor 等
- 支持 **LangChain Middleware**：原生集成到你的 Agent 应用
- **Docker 隔离执行**：安全可靠，开箱即用
- **渐进式披露**：轻量加载，按需读取

```
┌─────────────────────────────────────────────────────────────┐
│                      你的 AI Agent                          │
│                                                             │
│  ┌─────────────┐  ┌─────────────┐  ┌─────────────┐         │
│  │ LangChain   │  │ Claude      │  │ 自定义      │         │
│  │ Agent       │  │ Desktop     │  │ Agent       │         │
│  └──────┬──────┘  └──────┬──────┘  └──────┬──────┘         │
│         │                │                │                 │
│         └────────────────┼────────────────┘                 │
│                          │                                  │
│                          ▼                                  │
│              ┌───────────────────────┐                      │
│              │    Agent Skills       │                      │
│              │  ┌─────┐ ┌─────────┐  │                      │
│              │  │ MCP │ │Middleware│ │                      │
│              │  └─────┘ └─────────┘  │                      │
│              └───────────────────────┘                      │
│                          │                                  │
│                          ▼                                  │
│              ┌───────────────────────┐                      │
│              │   Skills 生态系统     │                      │
│              │  PDF处理 | 代码审查   │                      │
│              │  数据分析 | 自定义... │                      │
│              └───────────────────────┘                      │
└─────────────────────────────────────────────────────────────┘
```

---

## 功能特性

- **统一工具集**：6 个 `skills_*` 工具，功能原子化，易于理解
- **Docker 隔离执行**：在容器中运行命令，预装常用工具和库
- **双重集成方式**：
  - MCP 协议：兼容 Claude Desktop、Cursor 等
  - LangChain Middleware：原生 Python 集成，更低延迟
- **渐进式披露**：技能元数据预加载，完整内容按需读取
- **元技能自动复制**：自定义 skills 目录时自动获得 `skill-creator`

---

## 内置技能

除了核心框架能力，我们还提供了几个精心设计的技能，开箱即用：

| 技能 | 描述 | 亮点 |
|------|------|------|
| 🛠️ **skill-creator** | 元技能，教你如何创建新技能 | 完整的创建指南和模板 |
| 📄 **pdf** | 综合 PDF 处理工具集 | 文本提取、表格解析、合并分割、表单填写 |
| 🌐 **website_design** | 网页设计系统 | 提供 Monochrome、Bauhaus 等独特风格规范 |
| ⬇️ **file-downloader** | 文件下载器 | 支持 HTTP/HTTPS，自动检测文件名 |

> 🌟 **我们期待你创建更多有趣的技能！** 使用 `skill-creator` 元技能，你可以轻松封装自己的专业知识和工具。如果你创建了实用的技能，欢迎贡献到社区。

---

## 快速开始

### 1. 构建 Docker 镜像

```bash
docker build -t agent-skills:latest -f docker_config/Dockerfile .
```

### 2. 选择集成方式

**方式 A：Claude Desktop / Cursor（MCP）**

编辑 `~/Library/Application Support/Claude/claude_desktop_config.json`:

```json
{
  "mcpServers": {
    "agent-skills": {
      "command": "docker",
      "args": ["run", "-i", "--rm",
               "-v", "~/.agent-skills/skills:/skills",
               "-v", "/Users:/Users",
               "agent-skills:latest"]
    }
  }
}
```

两个挂载的作用：
- `~/.agent-skills/skills:/skills` - skills 目录（必需），注意修改为你想存储skills的目录
- `/Users:/Users` - 宿主机文件访问（可选，用于脚本读写外部文件）

> 💡 **提示**：如果想在使用 MCP 时获得与 Middleware（可注入系统提示词）同样的效果，可以在项目根目录中添加 `.cursor/rules/python/skills_prompt.mdc` 文件，这能更好地指导 Cursor 使用技能。详见 [MCP 集成文档](mcp-integration_cn.md)。

> 💡 Linux 系统请将 `/Users:/Users` 改为 `/home:/home`

**方式 B：LangChain 应用（Middleware）**

```python
from agent_skills.core.middleware import SkillsMiddleware
from deepagents import create_deep_agent

# 配置 skills_dir 和宿主机目录挂载
middleware = SkillsMiddleware(
    skills_dir="/path/to/skills",
    host_mount="/Users:/Users",  # 可选，用于脚本访问外部文件
)

agent = create_deep_agent(
    tools=[],
    system_prompt="You are a helpful assistant.",
    middleware=middleware.get_middlewares(),
)
```

---

## 文档

| 文档 | 说明 |
|------|------|
| [工具参考](tools_cn.md) | 6 个 `skills_*` 工具详解 |
| [MCP 集成](mcp-integration_cn.md) | Claude Desktop / Cursor 配置 |
| [Middleware 集成](middleware-integration_cn.md) | LangChain 原生集成 |
| [示例 Demo](examples_cn.md) | 4 个示例程序说明 |
| [Docker 环境](docker-environment_cn.md) | 预装工具和环境变量 |
| [Skill 格式](skill-format_cn.md) | 如何编写和组织 Skill |

---

## 开发

```bash
# 安装依赖
uv sync

# 运行测试
uv run pytest tests/ -v

# 本地启动 MCP Server
uv run agent-skills-server
```

---

## License

Apache 2.0
